# Pinned so rendered output (SVG masters, WebP art) stays reproducible across machines and CI.
cairosvg==2.9.0
fonttools==4.59.0
Pillow==10.4.0
# Wallpaper renderers use arrays for deterministic coverage metrics/composites.
numpy==2.4.6
# Self-contained renderer keeps output stable across hosts.
resvg-py==0.4.0
# docs/app-ui mockups: the auditor frame carries a real scannable QR of the
# generated deep link, encoded the same way AuditorActivity builds it.
qrcode==8.2

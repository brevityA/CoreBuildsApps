<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Sideload updater: lets the system installer prompt for "install unknown apps". -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Floating ticker: draw the chyron above other apps (granted via
         Settings.canDrawOverlays; blocked on Fire TV only). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.faketouch" android:required="false" />

    <!--
      Android 11+ package visibility: let the "Watch apps" picker enumerate
      installed launchable apps without the restricted QUERY_ALL_PACKAGES
      permission (queryIntentActivities for MAIN/LAUNCHER + LEANBACK_LAUNCHER).
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <!-- Explicit package visibility for known sports/streaming apps
             (Android 11+ hides packages not matched by intent queries). -->
        <package android:name="com.espn.score_center" />
        <package android:name="com.foxsports.android" />
        <package android:name="com.foxsports.videogo" />
        <package android:name="com.nbcsports.apps.tv" />
        <package android:name="com.peacocktv.peacockandroid" />
        <package android:name="com.cbs.ott" />
        <package android:name="com.cbs.ca" />
        <package android:name="com.dazn" />
        <package android:name="com.google.android.apps.youtube.unplugged" />
        <package android:name="com.hulu.livingroomplus" />
        <package android:name="com.sling" />
        <package android:name="tv.fubo.mobile" />
        <package android:name="com.amazon.amazonvideo.livingroom" />
        <package android:name="com.wbd.stream" />
        <package android:name="com.bellmedia.tsn" />
        <package android:name="com.rogers.sportsnet" />
        <package android:name="com.apple.atve.android.appletv" />
        <package android:name="com.sync.tv" />
        <!-- Niche / league-specific apps -->
        <package android:name="com.gotv.nflgamecenter.us.lite" />
        <package android:name="com.nbaimd.gametime.nba2011" />
        <package android:name="com.bamnetworks.mobile.android.gameday.atbat" />
        <package android:name="com.nhl.gc1112.free" />
        <package android:name="com.neulion.android.tablet.nfl.wnfln" />
        <package android:name="tv.flosports" />
        <package android:name="com.ballyscorp.ballylive" />
        <package android:name="com.fanduel.sportsbook" />
        <package android:name="com.draftkings.sportsbook" />
        <package android:name="com.bet365Wrapper.Bet365_Application" />
        <package android:name="com.bet365SportsCA.Bet365_Application" />
    </queries>

    <application
        android:allowBackup="true"
        android:banner="@drawable/tv_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoreLine"
        android:usesCleartextTraffic="true">

        <!-- Exposes cache/updates/ to the system package installer for the sideload updater. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Always-on-top sports ticker overlay (phone/tablet)" />
        </service>

        <activity
            android:name=".MainActivity"
            android:banner="@drawable/tv_banner"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden|navigation|smallestScreenSize|screenLayout"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTask"
            android:logo="@mipmap/ic_launcher"
            android:resizeableActivity="true"
            android:screenOrientation="fullUser"
            android:theme="@style/Theme.CoreLine">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>

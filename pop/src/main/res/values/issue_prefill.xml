<?xml version="1.0" encoding="utf-8"?>
<!-- Generated by tools/build_issue_prefills.py. Do not edit by hand. -->
<resources>
    <!-- On-device auditor deep link. %1$s = app label (URL-encoded,
         also completes the title), %2$s = component, %3$s = device
         note. The app encodes each argument before substituting.
         Read with getString(id, args), i.e. through String.format:
         literal percents are written %% (the Formatter escape - a
         bare %5B parses as width 5 + boolean conversion and throws),
         only the %n$s placeholders stay single. -->
    <string name="audit_issue_url_fmt">https://github.com/brevityA/CoreBuildsApps/issues/new?assignees=&amp;labels=icon+request&amp;projects=&amp;template=1.new_icon_request.yml&amp;title=%%5BIcon%%5D+%1$s&amp;app_name=%1$s&amp;component=%2$s&amp;notes=%3$s</string>
    <!-- The same link for an app the pack maps under another
         activity: the icon exists and is not applying, so it opens
         the not-applying form. Same three arguments, same escaping. -->
    <string name="audit_mapping_url_fmt">https://github.com/brevityA/CoreBuildsApps/issues/new?assignees=&amp;labels=mapping&amp;projects=&amp;template=2.icon_not_applying.yml&amp;title=%%5BNot+applying%%5D+%1$s&amp;app_name=%1$s&amp;component=%2$s&amp;device=%3$s</string>
    <!-- Anonymous-request broker. A row press POSTs to it before
         the QR panel is even offered; empty means not deployed and
         the press goes straight to the QR panel. Bake the deployed
         URL in with the endpoint flag on this generator once
         tools/icon_request_broker is live. (XML comments ban
         doubled hyphens, so the flag is unnamed here; the broker
         README names it.) -->
    <string name="audit_request_endpoint"></string>
    <!-- QR interstitial on this repo's GitHub Pages
         (docs/icon-request/index.html). Empty means the QR is the
         direct GitHub deep link; baked, the QR lands on the page
         first - always rendered, browser-borne, and able to send
         straight to the broker on phones with no GitHub login.
         Bake with the landing flag on this generator once Pages
         serves /docs from main. (Flag unnamed here; same XML rule.) -->
    <string name="audit_qr_landing"></string>
</resources>

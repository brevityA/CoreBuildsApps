*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
# Unanchored on purpose: shift/ and motion-plugin/ are standalone Gradle roots,
# so /build and /app/build alone left their outputs untracked-but-not-ignored.
build/
captures/
.gradle/
/captures
.externalNativeBuild
.cxx
local.properties
keystore.properties
*.jks
*.keystore
__pycache__/

# Device scans are per-machine, not repo state
tools/device_scan.json
tools/device_scan.txt

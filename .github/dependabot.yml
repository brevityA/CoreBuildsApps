# GENERATED FILE — do not edit by hand.
#
#     python tools/build_dependabot.py           # regenerate
#     python tools/build_dependabot.py --check   # what CI runs
#
# The `ignore:` blocks below are derived from tools/gradle_envelope.json and
# from which coordinates each Gradle root actually declares. They are the only
# thing stopping Dependabot proposing versions that cannot compile against this
# suite's compileSdk / minSdk / Kotlin — Dependabot reads version numbers and
# cannot see any of those. So they must not be able to drift or be quietly
# deleted, which is exactly what happened in 9f5d266d and what produced the four
# red PRs #122 #125 #126 #132.
#
# Capped coordinates block minor and major updates only; patch releases still
# flow. Each cap records its reason and its evidence in gradle_envelope.json.
#
# To take a capped dependency forward: change the envelope first (migration
# steps are at the end of gradle_envelope.json), watch CI go green, then lift
# the ceiling and regenerate this file.
#
# Note on the `gradle-wrapper >= 9.6` rule and the `com.android.application`
# ceiling below. They are two halves of ONE constraint, and the 2026-09-19
# Dependabot batch (#144 #145 #146 #147 #148 #149) is what settled it. Gradle
# 9.6.0 removed org.gradle.api.problems.internal.InternalProblems
# (gradle/gradle#38073) and AGP 8.13.2 still binds it, so AGP 8.13.2 on the
# 9.7.0 wrapper that five of six roots run dies at plugin apply - before a
# single line compiles. #146 moved its wrapper to 9.5.1 with the same AGP
# 8.13.2 and built green, so AGP 8.13.2 is not broken by itself, only in that
# combination. AGP 8.5.2 runs on 9.7.0, which is why main is green today: that
# is one AGP version tolerating the removal, not AGP 8.x as a whole. The AGP
# ceiling is therefore 8.5.2, not 9, until the wrappers move or AGP 9 lands.
version: 2
updates:

  # . — repo root
  - package-ecosystem: "gradle"
    directory: "/"
    schedule:
      interval: "weekly"
    ignore:
      # Carried over from #121 — read the note in the header before trusting it.
      - dependency-name: "gradle-wrapper"
        versions: [">=9.6"]
      # The same wrapper under the other name Dependabot sometimes reports.
      - dependency-name: "org.gradle"
        versions: [">=9.6"]
      # com.android.application <= 8.5.2
      - dependency-name: "com.android.application"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.core:core-ktx <= 1.13.1 — declared by app, pop
      - dependency-name: "androidx.core:core-ktx"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.appcompat:appcompat <= 1.7.0 — declared by app, pop
      - dependency-name: "androidx.appcompat:appcompat"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.recyclerview:recyclerview <= 1.3.2 — declared by app, pop
      - dependency-name: "androidx.recyclerview:recyclerview"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # org.jetbrains.kotlin.android <= 1.9.24
      - dependency-name: "org.jetbrains.kotlin.android"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
    groups:
      android-root:
        patterns: ["*"]

  # pixel-neon — pixel-neon/
  - package-ecosystem: "gradle"
    directory: "/pixel-neon"
    schedule:
      interval: "weekly"
    ignore:
      # Carried over from #121 — read the note in the header before trusting it.
      - dependency-name: "gradle-wrapper"
        versions: [">=9.6"]
      # The same wrapper under the other name Dependabot sometimes reports.
      - dependency-name: "org.gradle"
        versions: [">=9.6"]
      # com.android.application <= 8.5.2
      - dependency-name: "com.android.application"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.core:core-ktx <= 1.13.1 — declared by app
      - dependency-name: "androidx.core:core-ktx"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.appcompat:appcompat <= 1.7.0 — declared by app
      - dependency-name: "androidx.appcompat:appcompat"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.recyclerview:recyclerview <= 1.3.2 — declared by app
      - dependency-name: "androidx.recyclerview:recyclerview"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # org.jetbrains.kotlin.android <= 1.9.24
      - dependency-name: "org.jetbrains.kotlin.android"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
    groups:
      pixel-neon:
        patterns: ["*"]

  # ticker/android — ticker/android/
  - package-ecosystem: "gradle"
    directory: "/ticker/android"
    schedule:
      interval: "weekly"
    ignore:
      # Carried over from #121 — read the note in the header before trusting it.
      - dependency-name: "gradle-wrapper"
        versions: [">=9.6"]
      # The same wrapper under the other name Dependabot sometimes reports.
      - dependency-name: "org.gradle"
        versions: [">=9.6"]
      # com.android.application <= 8.5.2
      - dependency-name: "com.android.application"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.core:core-ktx <= 1.13.1 — declared by app
      - dependency-name: "androidx.core:core-ktx"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # org.jetbrains.kotlin.android <= 1.9.24
      - dependency-name: "org.jetbrains.kotlin.android"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
    groups:
      core-line-android:
        patterns: ["*"]

  # shift — shift/
  - package-ecosystem: "gradle"
    directory: "/shift"
    schedule:
      interval: "weekly"
    ignore:
      # Carried over from #121 — read the note in the header before trusting it.
      - dependency-name: "gradle-wrapper"
        versions: [">=9.6"]
      # The same wrapper under the other name Dependabot sometimes reports.
      - dependency-name: "org.gradle"
        versions: [">=9.6"]
      # com.android.application <= 8.5.2
      - dependency-name: "com.android.application"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.core:core-ktx <= 1.13.1 — declared by app
      - dependency-name: "androidx.core:core-ktx"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.appcompat:appcompat <= 1.7.0 — declared by app
      - dependency-name: "androidx.appcompat:appcompat"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.recyclerview:recyclerview <= 1.3.2 — declared by app
      - dependency-name: "androidx.recyclerview:recyclerview"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.media3:media3-exoplayer <= 1.4.1 — declared by app
      - dependency-name: "androidx.media3:media3-exoplayer"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.media3:media3-ui <= 1.4.1 — declared by app
      - dependency-name: "androidx.media3:media3-ui"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.media3:media3-session <= 1.4.1 — declared by app
      - dependency-name: "androidx.media3:media3-session"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # org.jetbrains.kotlin.android <= 1.9.24
      - dependency-name: "org.jetbrains.kotlin.android"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
    groups:
      core-shift:
        patterns: ["*"]

  # motion-plugin — motion-plugin/
  - package-ecosystem: "gradle"
    directory: "/motion-plugin"
    schedule:
      interval: "weekly"
    ignore:
      # Carried over from #121 — read the note in the header before trusting it.
      - dependency-name: "gradle-wrapper"
        versions: [">=9.6"]
      # The same wrapper under the other name Dependabot sometimes reports.
      - dependency-name: "org.gradle"
        versions: [">=9.6"]
      # com.android.application <= 8.5.2
      - dependency-name: "com.android.application"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.core:core-ktx <= 1.13.1 — declared by app
      - dependency-name: "androidx.core:core-ktx"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.appcompat:appcompat <= 1.7.0 — declared by app
      - dependency-name: "androidx.appcompat:appcompat"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # org.jetbrains.kotlin.android <= 1.9.24
      - dependency-name: "org.jetbrains.kotlin.android"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # org.jetbrains.kotlin.plugin.parcelize <= 1.9.24
      - dependency-name: "org.jetbrains.kotlin.plugin.parcelize"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
    groups:
      core-motion:
        patterns: ["*"]

  # doctor — doctor/
  - package-ecosystem: "gradle"
    directory: "/doctor"
    schedule:
      interval: "weekly"
    ignore:
      # Carried over from #121 — read the note in the header before trusting it.
      - dependency-name: "gradle-wrapper"
        versions: [">=9.6"]
      # The same wrapper under the other name Dependabot sometimes reports.
      - dependency-name: "org.gradle"
        versions: [">=9.6"]
      # com.android.application <= 8.5.2
      - dependency-name: "com.android.application"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.appcompat:appcompat <= 1.7.0 — declared by app
      - dependency-name: "androidx.appcompat:appcompat"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.compose:compose-bom <= 2024.06.00 — declared by app
      - dependency-name: "androidx.compose:compose-bom"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # androidx.activity:activity-compose <= 1.9.0 — declared by app
      - dependency-name: "androidx.activity:activity-compose"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # org.jetbrains.kotlinx:kotlinx-serialization-json <= 1.6.3 — declared by app
      - dependency-name: "org.jetbrains.kotlinx:kotlinx-serialization-json"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # org.jetbrains.kotlinx:kotlinx-coroutines-android <= 1.8.1 — declared by app
      - dependency-name: "org.jetbrains.kotlinx:kotlinx-coroutines-android"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # org.jetbrains.kotlin.android <= 1.9.24
      - dependency-name: "org.jetbrains.kotlin.android"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
      # org.jetbrains.kotlin.plugin.serialization <= 1.9.24
      - dependency-name: "org.jetbrains.kotlin.plugin.serialization"
        update-types: ["version-update:semver-minor", "version-update:semver-major"]
    groups:
      core-doctor:
        patterns: ["*"]

  - package-ecosystem: "npm"
    directory: "/ticker"
    schedule:
      interval: "weekly"
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
